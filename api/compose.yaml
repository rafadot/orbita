services:
  mailpit:
    image: axllent/mailpit:latest
    ports:
      - "1025:1025"   # SMTP — usado pelo spring-boot-starter-mail em dev
      - "8025:8025"   # UI web para visualizar os e-mails enviados
